// Root page now serves the marketing landing page
// The actual landing page component is in (marketing)/page.tsx
export { default } from './(marketing)/page'
export { metadata, dynamic } from './(marketing)/page'
