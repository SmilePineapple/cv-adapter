fix: Show full CV content in all templates - remove limits and fallbacks

Comprehensive template fixes to display complete CV content:

1. Helper Functions ✅
   - extractSkills(): Removed .slice(0, 8) → Shows ALL skills
   - extractLanguages(): Removed .slice(0, 3) → Shows ALL languages
   - extractHobbies(): Removed .slice(0, 3) → Shows ALL hobbies

2. Professional Metrics Template ✅
   - Removed .slice(0, 3) on experience → Shows ALL jobs
   - Removed || 'Description' fallback
   - Removed .slice(0, 3) on languages

3. Teal Sidebar Template ✅
   - Removed .slice(0, 2) on experience → Shows ALL jobs
   - Removed || 'Position', || 'Company', || 'Description' fallbacks
   - Removed .slice(0, 5) on skills → Shows ALL skills

4. Soft Header Template ✅
   - Removed .slice(0, 2) on experience → Shows ALL jobs
   - Removed || 'Position', || 'Company', || 'Description' fallbacks
   - Removed .slice(0, 6) on skills → Shows ALL skills

5. Artistic Header Template ✅
   - Removed .slice(0, 2) on experience → Shows ALL jobs
   - Removed .slice(0, 3) on languages

6. Bold Split Template ✅
   - Removed .slice(0, 1) on experience → Shows ALL jobs
   - Removed .slice(0, 3) on education lines
   - Removed .slice(0, 6) on skills → Shows ALL skills
   - Removed .slice(0, 3) on languages
   - Removed .slice(0, 4) on skills slider

Impact:
- Templates now show complete CV content
- No more truncated experience/skills
- No more placeholder text showing
- Full multi-page CVs will render properly

Files Modified:
- src/lib/stunning-templates.ts (all 5 templates)

Status: Build passing, ready to deploy!
